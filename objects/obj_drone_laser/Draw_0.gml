draw_surface(laserSurf, 0, 0); //the actual laser

draw_sprite(spr_drone_laser_end,alarm[0],targetX,targetY)

//draw_sprite(slaser, 0, x, y); // draw the laser or for projectile testing
//draw_line(startX - 1, startY - 1, x - 1, y - 1); // for collision testing