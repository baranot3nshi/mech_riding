/// @description Insert description here
// You can write your code in this editor

// Inherit the parent event
event_inherited();

dmg = 1;
pushback = 0;
reflect = true;
audio_sound_pitch(snd_sword,1)
audio_play_sound(snd_sword,5,false);